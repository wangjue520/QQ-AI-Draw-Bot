# QQ Anima 跑图机器人 · 完整教程

> 适用版本：本教程对应当前部署（Web 控制台版）。配套阅读：项目根目录《使用说明.md》。

---

## 一、这套系统是什么

```
你在群里 @小号 发"画一个佩丽卡"
        │
        ▼
   ┌─────────┐   WebSocket(8080)   ┌──────────────┐
   │  QQ 小号 │ ◄────────────────► │  机器人 bot.py │
   └─────────┘   (NapCat 托管)     │  (本项目目录)  │
                                   └───┬───────┬──┘
                            提示词优化   │       │  提交跑图任务
                                       ▼       ▼
                               ┌─────────┐  ┌────────────┐
                               │ xAI API │  │ Forge 7860 │ ◄── 你也在网页里手点生成
                               │ (grok)  │  │  (本地AI)   │     （你的永远优先）
                               └─────────┘  └────────────┘
                                        │
                                        ▼
                              图发回群里 + Web 控制台留档
```

四个组件，缺一不可：

| 组件 | 是什么 | 在哪 |
|---|---|---|
| **Forge** | 本地 AI 画图软件（WebUI） | `F:\sd-forge-aki\sd-webui-forge-aki-v1.0\sd-webui-forge-aki`（端口 7860） |
| **NapCat** | QQ 协议端，托管小号收发消息 | 项目内 `NapCat\Shell`（WebUI 端口 6099） |
| **机器人** | 本项目 bot.py，调度一切 | 项目目录（QQ 端口 8080，控制台 8081） |
| **xAI** | 提示词优化 + 模糊分辨率判断 | 云端 API，key 在控制台设置 |

**机器人 QQ（小号）**：177144386（"ftp不到300w就不改名王王玉"）。大号"在下王王玉"只在自己 QQ 里用，不托管。

---

## 二、日常开关机（最重要）

### 启动 —— 只需双击一个文件

**双击 `启动机器人.bat`**，它会自动按顺序处理：

1. 检查 Python 和依赖
2. Forge 没开就最小化启动（模型加载约 3 分钟，加载完才能出图）
3. NapCat 没开就最小化启动，并**自动登录小号（免扫码）**
4. 启动机器人本体

启动成功的标志（机器人窗口里依次出现）：

```
[Web] 控制台已启动：http://127.0.0.1:8081
[QQ] 等待 NapCat 连接：ws://127.0.0.1:8080
[QQ] NapCat 已接入 ('127.0.0.1', xxxxx)
```

看到 `NapCat 已接入` 就能用了。Forge 还在加载也不影响发任务，任务会排队等它。

### 关闭

- **关机器人**：关掉机器人窗口，或双击 `停止机器人.bat`（只杀 bot.py，不误伤其他 Python 程序）
- **Forge / NapCat**：可以一直挂着；要关就关掉它们的最小化窗口
- **关机电脑前**：不用特意关什么，直接关机即可（下次双击 bat 全自动恢复）

### 重启电脑后的顺序

直接双击 `启动机器人.bat` 就行，不用手动开 Forge/NapCat。唯一例外：如果小号在手机上把 NapCat 设备踢下线了，需要重新扫码（见常见问题）。

---

## 三、群里怎么使唤机器人

群聊 **@小号 + 指令**，私聊小号直接发（不用 @）。

### 核心指令

| 指令 | 效果 |
|---|---|
| `@小号 画 画面描述` | AI 优化提示词 + 自动触发角色 LoRA + 跑图 |
| `@小号 画 厚涂 横 一个少女` | 画风 + 构图自由组合 |
| `@小号 生图 英文tag` | 跳过优化直接跑（给会写 tag 的人） |
| `@小号 再来` | 用上一条提示词换种子重 roll |
| `@小号 画风` | 列出所有画风 |
| `@小号 帮助` | 使用说明 |

### 尺寸写法

| 写法 | 结果 |
|---|---|
| `画 1000x1400 少女` | **精准尺寸**，直接生效，不调用 AI |
| `画 1080p 少女` / `2k` / `4k` / `720p` | **模糊说法**，AI 换算成模型能跑的尺寸（群里会提示选定结果） |
| `画 横 少女` / `竖` / `方` | 固定构图 1344x768 / 768x1344 / 1024x1024 |
| 组合 `画 横 1080p 少女` | 横竖关键词优先 |

规则：精准 > 横竖方 > 模糊；模糊词不会混进提示词。

### 优先级（重要）

**你在 Forge 网页里手点生成的图永远最优先**。机器人在提交前会探测 Forge：你在跑它就安静排队，并提示"Forge 正在跑本地任务，排队等待中…"；你一跑完它自动接上。反过来机器人在跑时你在网页点生成，会排在机器人后面。

### 多人同时用

多个任务自动排队，群里会报"已加入队列，前面还有 N 个任务"。单张卡一次一张，按提交顺序来。

---

## 四、Web 控制台（http://127.0.0.1:8081）

机器人启动后浏览器自动打开，以后随时双击启动器也会打开。所有配置都在网页里改，保存即生效。

| 标签页 | 功能 |
|---|---|
| **跑图 / 队列** | 手动跑图（不开 QQ 测试）；**Forge 实时进度条 + 预览图**；队列状态；历史出图（点图看大图） |
| **画风预设** | 增删改画风（正向/负向/介绍），群里指令直接生效 |
| **LoRA 管理** | 扫描结果、触发词、中文别名、权重、启用开关 |
| **角色字典** | 520+ 条中文名→D站tag，搜索/新增/删除/**批量导入** |
| **优化提示词** | 改发给大模型的 system prompt |
| **设置** | xAI key、代理、Forge 地址、LoRA 目录、步数/CFG/采样器、质量前缀、冷却 |
| **Forge WebUI** | **整个 Forge 嵌在控制台里**，可直接操作；右上可新标签页打开 |
| **QQ 账号** | 看机器人绑在哪个 QQ；**一键切换账号**（免扫码）；**设置一键启动时自动登录的账号** |

### LoRA 自动触发原理（回顾）

1. 群友说"画一个**佩丽卡**" → 字典命中 → tag `perlica (arknights)`
2. AI 优化提示词时强制使用该 tag
3. 机器人扫描所有 LoRA 的 `.civitai.info`，发现终末地 LoRA 训练词里有它 → 自动拼 `<lora:endfield_all_v3-000012:0.8>`
4. 群里提示"自动触发 LoRA：xxx"

对不上时：给 LoRA 加**中文别名**，或在字典里补角色名。单张图最多挂几个、默认权重，在"设置"里调。

---

## 五、常见问题（按症状排查）

### 机器人完全没反应

1. 看机器人窗口是不是开着（关了 = 下线）
2. 控制台首页看 QQ 状态；如果显示没接入 → NapCat 没开或没登录 → 重跑启动器，或单独开 NapCat
3. 群里 @的号对不对（要 @小号 177144386）

### 卡在 `[QQ] 等待 NapCat 连接`

- NapCat 没启动/已崩溃：重跑一次 `启动机器人.bat`（它会自动补起 NapCat 并自动登录）
- 别去任务管理器只杀 NapCat 的主进程——会留下一堆孤儿 QQ 进程导致快速登录卡死（详见下条）

### 小号登录失败 / 快速登录一直转圈

- 原因多半是之前杀 NapCat 时没杀干净，残留的 QQ 进程在抢登录状态
- 解决：任务管理器里把今天新出现的 `QQ.exe` 进程全部结束（保留你自己大号那个），然后重跑启动器
- 终极手段：打开 `http://127.0.0.1:6099/webui?token=25c9e05d48ed`（NapCat 控制台），点快速登录或扫码

### 重新扫码

NapCat 的二维码在：① 小号独立 QQ 窗口里（自动刷新）；② `NapCat\Shell\cache\qrcode.png`。手机扫完**必须在手机上点"登录"按钮**。

### 提示词优化失败 / 提示"还没填大模型 key"

- 控制台 → 设置 → 检查 xAI 的 key（格式是 `xai-` 开头一串）
- key 在 https://console.x.ai 生成；国内网络直连即可，代理一般不用填
- key 错了不影响跑图，只是跳过优化直接用原文

### 图片发不出 / 发得慢

升级 NapCat；群里发图受 QQ 风控限制，别刷屏，等几秒

### 端口冲突（8080 / 8081 / 7860 / 6099 被别的程序占用）

- 控制台"设置"里可改 QQ 端口和 Forge 地址；NapCat 的 WS 客户端地址也要跟着改（NapCat 控制台 → 网络配置）
- Forge 被占就关多余的 Python

### Forge 启动没反应

直接双击 `F:\sd-forge-aki\sd-webui-forge-aki-v1.0\sd-webui-forge-aki\webui-user.bat`（--api 已内置，任何启动方式都带 API）。整合包启动器偶尔抽风，用 bat 最稳。

---

## 六、进阶玩法

### 换一个小号托管

1. 新号先在 NapCat 里扫码登录一次（让它进快速登录列表）
2. 之后控制台"QQ 账号"页随时一键切换

### 添加新 LoRA

把 LoRA 文件放进 `F:\sd-forge-aki\sd-webui-forge-aki-v1.0\sd-webui-forge-aki\models\Lora`，旁边带上 `.civitai.info`（C站下载自带），控制台"LoRA 管理"点"重新扫描"。

### 批量补角色字典

控制台"角色字典"页，批量导入框每行一条：

```
tag(下划线写法),作品名,中文名,别名1,别名2
amiya_(arknights),明日方舟,阿米娅,兔兔
```

### 改画风

控制台"画风预设"页。正向里写 LoRA 用 `<lora:文件名:权重>`，画师用 `@画师名`。保存后群里立即生效。

### 手动开各组件（启动器失效时的备用）

```
Forge:   F:\sd-forge-aki\...\webui-user.bat
NapCat:  项目目录\NapCat\Shell\launcher.bat（右键管理员运行）
机器人:   项目目录\启动机器人.bat
```

---

## 七、文件速查

| 文件 | 作用 |
|---|---|
| `启动机器人.bat` | 一键启动 Forge+NapCat+机器人 |
| `停止机器人.bat` | 只杀机器人的 Python 进程 |
| `napcat_auto_login.py` | 启动器调用的自动登录小号脚本 |
| `data\config.json` | 全部配置（网页里改的就是它） |
| `data\char_dict.json` | 角色字典 |
| `data\presets.json` | 画风预设 |
| `outputs\` | 出图存档 |
| `NapCat\Shell\` | NapCat 本体 |
| `web\index.html` | 控制台页面 |
| `bot.py / core.py / qq.py / panel.py` | 机器人源码 |
