# QQ Anima 跑图机器人 使用说明（v2 Web 控制台版）

群里 **@机器人 + 指令 + 画面描述**，机器人先用 AI 把大白话优化成 Anima 规范提示词，识别提到的角色并**自动触发对应 LoRA**，再调你电脑上的 WebUI Forge 跑图发回群里。自带 **Web 控制台**，所有配置、画风、字典都在网页里改，不用碰代码。

---

## 一、部署（总共 4 步）

1. **装 Python 3.10+**（安装时勾选 Add python.exe to PATH）
2. **装 NapCat** 并登录 QQ 小号，网络配置里新建 **WebSocket 客户端**，URL 填 `ws://127.0.0.1:8080`，消息格式选 Array
3. **Forge 启动参数加 `--api`**（编辑 webui-user.bat 的 `COMMANDLINE_ARGS`）
4. 双击 **启动机器人.bat**，浏览器会自动打开控制台 `http://127.0.0.1:8081`

启动顺序建议：Forge（等模型加载完）→ NapCat → 机器人。

## 二、Web 控制台（重点）

| 标签页 | 能干什么 |
|---|---|
| 跑图/队列 | 不开 QQ 直接测试跑图；实时看当前任务和排队情况；历史出图缩略图 |
| 画风预设 | 网页里增删改画风（正向串/负向/介绍），保存即生效 |
| LoRA 管理 | 扫描 LoRA 目录，看每个 LoRA 的角色触发词，改中文别名、权重、开关 |
| 角色字典 | 520+ 条「中文名→D站tag」，可搜索、新增、删除 |
| 优化提示词 | 修改发给大模型的 system prompt（内置 Anima 官方规则），保存即生效 |
| 设置 | xAI key、代理、LoRA 目录、步数/CFG/采样器、质量前缀、冷却等 |

**第一次用必做**：设置页填 ① LoRA 目录（Forge 的 `models\Lora` 完整路径）② xAI 的 API Key（国内不通就顺手填代理）。

## 三、LoRA 自动触发的原理

你在 C 站下载 LoRA 时旁边的 `.civitai.info` 文件里存着它的训练角色 tag（比如 `perlica (arknights)`）。机器人会：

1. 群友说"画一个**佩丽卡**"→ 字典命中 → tag 是 `perlica_(arknights)`
2. AI 优化提示词时强制使用这个 tag
3. 扫描所有 LoRA 的 civitai.info，发现终末地 LoRA 的训练词里有 `perlica (arknights)` → **自动拼上 `<lora:endfield_all_v3-000012:0.8>`**
4. 群里会提示"自动触发 LoRA：xxx"

如果某个 LoRA 的触发词对不上中文名（或字典里没收录），在「LoRA 管理」里给它加**中文别名**即可。单张图最多自动挂几个 LoRA、默认权重，都能在设置里调。

## 四、群里指令

| 指令 | 效果 |
|---|---|
| `@机器人 画 画面描述` | AI 优化 + 自动触发 LoRA + 跑图 |
| `@机器人 画 厚涂 横 一个少女` | 画风 + 构图（横/竖/方 或 1344x768）随意组合 |
| `@机器人 生图 英文tag` | 不优化直接跑（给会写 tag 的人） |
| `@机器人 再来` | 用上条提示词换种子重 roll |
| `@机器人 画风` / `帮助` | 列表 / 说明 |

私聊机器人也能用（不用 @）。单卡一次只能跑一张，多任务自动排队并在群里报位置。

## 五、生成参数参考（Anima 官方建议）

- **Base / Aesthetic**：30~50 步，CFG 4~6（Aesthetic 可到 3），采样器 Euler a / er_sde
- **Turbo**：8~12 步，CFG **1**
- 分辨率 512~1536；用 Aesthetic 版时把质量前缀里的 `score_7` 删掉

## 六、常见问题

- **机器人没反应**：控制台首页看 QQ 是否接入；NapCat 的 WS 客户端 URL/端口是否对上
- **图片发不出**：升级 NapCat；风控问题换小号
- **优化失败**：xAI 要代理，设置页填 `http://127.0.0.1:7890` 之类；填不了就留空 key，机器人会跳过优化
- **LoRA 没触发**：确认 ① 设置里 LoRA 目录正确 ② LoRA 旁边有 .civitai.info ③ 该 LoRA 在管理页是启用状态 ④ 角色名在字典里或该 LoRA 有别名
- **端口冲突**：8080（QQ）和 8081（控制台）被占了就在设置/配置里换
- **安全**：第三方协议端有封号风险，务必用小号
